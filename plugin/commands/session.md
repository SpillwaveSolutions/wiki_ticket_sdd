---
name: worklog-session
description: Open or close an isolated second-brain write session (worktree + PR).
---

Run the **worklog-session** skill.

User request: `$ARGUMENTS`

Follow `${CLAUDE_PLUGIN_ROOT}/skills/worklog-session/SKILL.md` completely. Prefer `${CLAUDE_PLUGIN_ROOT}/scripts/brain_session.py`. Never invent a remote URL. Never force-push.
