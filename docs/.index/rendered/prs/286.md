# PR #286

`pr/286` · status: **not tracked**

Changed files: not tracked. Test/Review status: not tracked — run `worklog pr-sync 286`.

## Linked Tickets

- [[Ticket-01KYZ5CY9PS60BYH1E07DF5C9P]] Compaction watermark can silently drop branch-local events the snapshot never folded — The fold drops any event whose id sorts at or below a compaction watermark, on the assumption that the compaction already folded it into a snapshot.
- [[Ticket-01KYZEC0C1MGQXQSH61ZPDD0YZ]] Conflict markers can reach a commit: the merge exemption plus unparsed tests — The commit-msg hook exempts merge commits from its item-reference rule, and the pre-commit hook only syntax-checks the event log and bin/ -- it never looks at tests/ or the plugin scripts.
- [[Ticket-01KYZER08G7NSQ6CFQ9QWFZFTJ]] Stamp generated ULIDs with the short git hash so concurrent branches diverge — Two agents working in different worktrees or branches mint event ids from the same clock and pure randomness, so ids created in the same millisecond sort arbitrarily and carry nothing about where they came from.
